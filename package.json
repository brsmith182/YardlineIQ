{
  "name": "yardlineiq",
  "version": "1.0.0",
  "description": "NFL picks and analytics platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "//dev": "Node does not read .env.local on its own — only Vercel does, and only for its own commands. Without this the local server boots with no ADMIN_PASSWORD or REDIS_URL. -if-exists so the script still runs when the file is absent. `start` stays bare: on Vercel the platform supplies the environment.",
    "dev": "nodemon --exec \"node --env-file-if-exists=.env.local server.js\""
  },
  "dependencies": {
    "express": "^4.18.2",
    "redis": "^4.6.0",
    "stripe": "^14.21.0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}
